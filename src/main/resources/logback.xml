<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	
	 <property name="LOG_HOME" value="applogs/waterboxserver" />
	  <appender name="STDOUT"
	            class="ch.qos.logback.core.ConsoleAppender">
	    <encoder>
				<pattern>
					%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
				</pattern>
	    </encoder>
	  </appender>
  
     <appender name="RollingFile"  
        class="ch.qos.logback.core.rolling.RollingFileAppender">  
  
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">  
            <level>TRACE</level>   
        </filter>  
  
        <!-- 可让每天产生一个日志文件，最多 10 个，自动回滚 -->  
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">  
            <fileNamePattern>${LOG_HOME}/logout-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>  
        </rollingPolicy>  
  
        <!-- 下面这段注释的代码可以在日志文件超过 10MB 时进行归档，并且归档文件后缀只要是 .zip 或 .gz 就会自动压缩日志归档 -->  
          <!--日志文件最大的大小-->
           <!-- 加了下面这段。不会每天生成一个文件 -->
     <!--    <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <MaxFileSize>10MB</MaxFileSize>
        </triggeringPolicy> -->
        <encoder>  
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger -- %msg -- %n</pattern>  
        </encoder>  
    </appender>
    
    <appender name="errorAppender"  
        class="ch.qos.logback.core.rolling.RollingFileAppender">  
  
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">  
            <level>TRACE</level>   
        </filter>  
  
        <!-- 可让每天产生一个日志文件，最多 2 个，自动回滚 -->  
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">  
            <fileNamePattern>${LOG_HOME}/errorlog-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>2</maxHistory>  
        </rollingPolicy>  
    <!-- 下面这段注释的代码可以在日志文件超过 10MB 时进行归档，并且归档文件后缀只要是 .zip 或 .gz 就会自动压缩日志归档 -->  
          <!--日志文件最大的大小-->
          <!-- 加了下面这段。不会每天生成一个文件 -->
      <!--   <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <MaxFileSize>10MB</MaxFileSize>
        </triggeringPolicy> -->
        <encoder>  
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger -- %msg -- %n</pattern>  
        </encoder>  
    </appender>
<!-- 	<logger name="s.d.s.w.PropertySourcedRequestMappingHandlerMapping" level="DEBUG"></logger> -->
	<logger name="org.hibernate" level="WARN"/> 
	<logger name="s.d.s.p.bean.BeanModelProperty" level="WARN"/> 
	<logger name="org.springframework" level="INFO"/> 
	<logger name="com.zaxxer.hikari.pool.HikariPool" level="WARN"/>
	<logger name="org.mybatis.spring.SqlSessionUtils" level="WARN"/>
    <logger name="o.m.s.t.SpringManagedTransaction" level="DEBUG" additivity="false">
    	<appender-ref ref="errorAppender" />
    </logger>
    <logger name="org.mybatis.spring.transaction.SpringManagedTransaction" level="ERROR" additivity="false">
    	<appender-ref ref="errorAppender" />
    </logger>

    <logger name="errorlog" level="DEBUG" additivity="false">
    	<appender-ref ref="errorAppender" />
    </logger>
	
	<root level="INFO">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="RollingFile" />
	</root>
	
	
</configuration>